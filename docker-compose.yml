
services:
  web:
    build: .
    image: password-generator:latest  
    container_name: password-generator
    ports:
      - "8080:5000"
    environment:
      - DEBUG=False
    restart: unless-stopped
